# Dev container — hot reload via Vite
FROM node:20-alpine

WORKDIR /app

# Instala deps primeiro (cache)
COPY package.json package-lock.json* ./
RUN npm install

# Código é montado via volume no docker-compose
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
